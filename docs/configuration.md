# Configuration

*To be written.*

Will document:
- Environment variables for cloud service
- Provider selection (which impl for LLM, STT, TTS, OCR, VectorStore, Storage, JobQueue)
- AWS region, role/credentials
- Default mode, default LLM model
- Rolling summary policy knobs
