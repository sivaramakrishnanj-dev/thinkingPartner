# Local Dev Stack (Docker)

**Status:** Not yet populated.

Will contain `docker-compose.yml` that brings up:
- Sarathi cloud service (Spring Boot app)
- Postgres 16 with `pgvector` extension
- (Optional) LocalStack for S3 / AWS service mocking in local dev

Usage (future):
```bash
docker compose up
```

Config via `.env` file (see `../../docs/configuration.md`).
