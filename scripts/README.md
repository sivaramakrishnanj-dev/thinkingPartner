# Scripts

Helper scripts (shell / Python) for dev workflows.

**Status:** Empty. Typical candidates once code exists:
- `build-all.sh` — build cloud service + Android app
- `run-local.sh` — bring up docker compose + run app
- `seed-db.sh` — seed built-in modes + dev data
- `package-release.sh` — produce release artifacts
